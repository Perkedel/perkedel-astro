---
export interface Props{
    inConfirmation?:Boolean;
    useCustomGoBack?:Boolean;
    customGoBack?:String;
}

const{ inConfirmation = false, useCustomGoBack = false, customGoBack = '../../..'} = Astro.props.frontmatter || Astro.props as Props;
---
<!-- Make the Poop button / return to SFW modular to be reusable on everything. -->
<!-- TODO: advanced detector of `enterNSFW/nsfw` & then throw that away thing -->
<a title={inConfirmation?"Throw the red pill to the toilet & then flush it":"Return to SFW version of this page"} href={useCustomGoBack ? customGoBack : (inConfirmation?"..":"../..")} class="NavBtn" id="PoopButton" style={""} set:html={"🚽 &#xe20e;"}></a>
<!-- I'm confused now!
- I think holding poop is discouraged! But then
- `Puke` / `Muntah` is disgusting and means a disease
- `Castract` / `Kebiri` is too dark and maybe a permanent desire loss
- `Poop` / `Berak` is natural
Then what & how am I supposed to philosophize this?
poop on command? maybe?!??!?
-->