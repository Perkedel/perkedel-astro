---
import BerakButton from "../BerakButton.astro";
export interface Props{
    inConfirmation?:Boolean;
}

const{ inConfirmation = false} = Astro.props.frontmatter || Astro.props as Props;
---
<!-- Flush NSFW. Return to SFW version. for us we go back 2 times (enterNSFW then regular sfw) -->
<li class="NavHistoryPutLeft"><BerakButton inConfirmation={inConfirmation}/></li>
